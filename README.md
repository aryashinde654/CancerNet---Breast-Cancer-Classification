# CancerNet VS Code Project
# CancerNet (IDC Classification) — VS Code Edition

## Quick Start
1. Create a venv and install deps:
   ```bash
   python -m venv .venv
   # Windows
   .venv\Scripts\activate
   # macOS/Linux
   source .venv/bin/activate

   pip install -r requirements.txt
